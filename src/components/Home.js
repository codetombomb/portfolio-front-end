import React from 'react';
import TomLogo from '../tom_logo.svg'

function Home(props) {
    return (
        <div id="home">
            <img src={TomLogo} />
        </div>
    );
}

export default Home;

//Diplay the tom logo